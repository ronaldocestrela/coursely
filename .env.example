# Copy to ".env" in the repo root for docker compose (never commit real secrets).

# SQL Server (must meet complexity rules for mssql/server image)
MSSQL_SA_PASSWORD=ChangeMe_Strong9!

# Browser-facing API URL baked into the frontend build (Docker build-arg override)
VITE_API_URL=http://localhost:5230
