name: Quality

on:
  push:
    branches: [main, master]
  pull_request:

concurrency:
  group: quality-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  backend-quality:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          # Align with repo root global.json sdk.version
          dotnet-version: '10.0.107'

      - name: Diagnostics (.NET + Docker)
        run: dotnet --info && docker version

      - name: Restore
        run: dotnet restore Coursely.slnx

      - name: Build
        run: dotnet build Coursely.slnx --configuration Release --no-restore

      - name: Test (with coverage + architecture suite)
        run: >-
          dotnet test Coursely.slnx
          --configuration Release
          --no-build
          --verbosity normal
          --collect:"XPlat Code Coverage"
          --results-directory "${{ github.workspace }}/TestResults"

      - name: Upload coverage reports
        if: success() || failure()
        uses: actions/upload-artifact@v4
        with:
          name: dotnet-coverage
          path: TestResults/**/*coverage*.xml
          if-no-files-found: error

  frontend-quality:
    runs-on: ubuntu-latest
    timeout-minutes: 35
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          # Match Dockerfile base and frontend/.nvmrc (approx. 20.19 latest patch).
          node-version: '20.19'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Diagnostics (Node + npm)
        run: node --version && npm --version

      - name: Install
        run: npm ci

      - name: Prettier check
        run: npm run format:check

      - name: Typecheck
        run: npm run typecheck

      - name: ESLint
        run: npm run lint

      - name: Vitest
        run: npm run test

      - name: Production build
        run: npm run build
